export type LoginUserProfile = {
	id : number
	username : string
	nickname : string
	avatarUrl : string
	lastLoginAt : string
}

export type LoginResponseData = {
	tokenType : string
	accessToken : string
	expiresIn : number
	user : LoginUserProfile | null
}

export type CurrentUser = {
	id : number
	username : string
	nickname : string
	avatarUrl : string
	mobile : string
	email : string
	profileCompleted : boolean
	studentId : number
	certificationStatus : string
}

export type CurrentUserPatch = {
	nickname? : string
	avatarUrl? : string
	mobile? : string
	email? : string
	profileCompleted? : boolean
	studentId? : number
	certificationStatus? : string
}

type ApiResponse<T> = {
	success : boolean
	code : string
	message : string
	data : T | null
}

const BASE_URL = 'https://api-test.arez.cc.cd'

export type WechatLoginData = {
	registered : boolean
	needBindMobile : boolean
	bindToken : string
	tokenType : string
	accessToken : string
	expiresIn : number
	user : LoginUserProfile | null
}

export type AppProfile = {
	id : number
	username : string
	mobile : string
	email : string
	nickname : string
	profileSignature : string
	avatarUrl : string
	authProvider : string
	gender : string
	status : string
	profileCompleted : boolean
	studentId : number
	certificationStatus : string
}

export type AppProfileUpdateRequest = {
	nickname? : string
	profileSignature? : string
	avatarUrl? : string
	email? : string
	gender? : string
}

export type AppFeedbackRequest = {
	feedbackType : string
	content : string
	contact : string
}

export type AppStudentCertification = {
	studentId : number
	studentNo : string
	realName : string
	mobile : string
	province : string
	city : string
	district : string
	organization : string
	positionTitle : string
	status : string
	certificationStatus : string
	certificationSubmittedAt : string
	certificationReviewedAt : string
	rejectReason : string
	certificationMaterials : string
	enrolledAt : string
}

export type AppStudentCertificationRequest = {
	realName : string
	mobile? : string
	idCardNo? : string
	province? : string
	city? : string
	district? : string
	organization? : string
	positionTitle? : string
	certificationMaterials? : string
}

export type LiveSessionVideo = {
	id : number
	liveSessionId : number
	title : string
	videoUrl : string
	durationSeconds : number
	sortOrder : number
	status : string
}

export type LiveSession = {
	id : number
	title : string
	coverUrl : string
	anchorName : string
	speakerName : string
	tags : string[]
	liveUrl : string
	playbackUrl : string
	startAt : string
	endAt : string
	reviewStatus : string
	liveStatus : string
	browseCount : number
	favoriteCount : number
	favorited : boolean
	videos : LiveSessionVideo[]
}

export type CourseVideo = {
	id : number
	courseId : number
	title : string
	videoUrl : string
	durationSeconds : number
	sortOrder : number
}

export type Course = {
	id : number
	courseName : string
	subtitle : string
	coverUrl : string
	lecturerName : string
	introduction : string
	paperId : number
	publishedAt : string
	progressPercent : number
	studySeconds : number
	videos : CourseVideo[]
}

export type AppPodcastAudio = {
	id : number
	podcastId : number
	title : string
	audioUrl : string
	durationSeconds : number
	sortOrder : number
}

export type AppPodcast = {
	id : number
	title : string
	summary : string
	coverUrl : string
	publishedAt : string
	progressPercent : number
	studySeconds : number
	audios : AppPodcastAudio[]
}

export type TopicItem = {
	id : number
	topicId : number
	itemType : string
	itemId : number
	sortOrder : number
	resource : UTSJSONObject | null
}

export type Topic = {
	id : number
	title : string
	summary : string
	learningRequirements : string
	coverUrl : string
	viewCount : number
	publishedAt : string
	items : TopicItem[]
}

export type TopicTag = {
	id : number
	tagName : string
}

export type TopicSectionResource = {
	resourceType : string
	resourceId : number
	title : string
	subtitle : string
	summary : string
	coverUrl : string
	browseCount : number
	favoriteCount : number
	favorited : boolean
	progressPercent : number
}

export type TopicSection = {
	sectionType : string
	sectionLabel : string
	total : number
	hasMore : boolean
	previewItems : TopicSectionResource[]
}

export type TopicCard = {
	id : number
	title : string
	summary : string
	learningRequirements : string
	coverUrl : string
	tags : string[]
	viewCount : number
	publishedAt : string
	favoriteCount : number
	favorited : boolean
}

export type TopicDetail = {
	id : number
	title : string
	summary : string
	learningRequirements : string
	coverUrl : string
	tags : string[]
	viewCount : number
	publishedAt : string
	favoriteCount : number
	favorited : boolean
	sections : TopicSection[]
}

export type Article = {
	id : number
	title : string
	summary : string
	coverUrl : string
	content : string
	authorName : string
	source : string
	tags : string[]
	viewCount : number
	favoriteCount : number
	favorited : boolean
	publishedAt : string
}

export type ExamQuestionOption = {
	id : number
	optionKey : string
	optionContent : string
	sortOrder : number
}

export type ExamQuestion = {
	questionId : number
	questionType : string
	title : string
	analysis : string
	difficulty : string
	score : number
	sortOrder : number
	options : ExamQuestionOption[]
}

export type ExamPaper = {
	id : number
	paperName : string
	description : string
	totalScore : number
	passScore : number
	durationMinutes : number
	status : string
	questionCount : number
	questions : ExamQuestion[]
}

export type ExamAnswerSubmitItem = {
	questionId : number
	answerContent : string
}

export type ExamSubmitRequest = {
	sourceType? : string
	sourceId? : number
	answers : ExamAnswerSubmitItem[]
}

export type ExamAnswerResult = {
	questionId : number
	questionType : string
	title : string
	answerContent : string
	correctAnswer : string
	analysis : string
	score : number
	correct : number
	options : ExamQuestionOption[]
}

export type ExamRecord = {
	id : number
	paperId : number
	paperName : string
	score : number
	passed : number
	totalScore : number
	passScore : number
	submittedAt : string
	answers : ExamAnswerResult[]
}

export type BrowseHistoryRequest = {
	resourceType : string
	resourceId : number
}

export type KnowledgeCategory = {
	id : number
	parentId : number
	categoryName : string
	categoryCode : string
	description : string
	sortOrder : number
	children : KnowledgeCategory[]
}

export type KnowledgeEntry = {
	id : number
	categoryId : number
	title : string
	summary : string
	categoryName : string
	categoryCode : string
	coverUrl : string
	content : string
	keywords? : string
	source? : string
	author? : string
	publisher? : string
	totalPages? : number
	publishedAt? : string
	viewCount? : number
	sortOrder? : number
}

export type AppBookChapter = {
	id : number
	bookId : number
	parentId : number
	chapterTitle : string
	content : string
	startPage : number
	pageCount : number
	paperId : number
	sortOrder : number
}

export type AppBookCategory = {
	id : number
	parentId : number
	categoryName : string
	sortOrder : number
}

export type AppBook = {
	id : number
	categoryId : number
	bookName : string
	author : string
	publisher : string
	coverUrl : string
	introduction : string
	totalPages : number
	paperId : number
	publishedAt : string
	browseCount : number
	favoriteCount : number
	favorited : boolean
	progressPercent : number
	studySeconds : number
	chapters : AppBookChapter[]
}

export type PageResponse<T> = {
	records : T[]
	total : number
	page : number
	size : number
}

export type AppResourceRecord = {
	id : number
	resourceType : string
	resourceId : number
	source : string
	viewCount : number
	occurredAt : string
}

export type LearningHistoryRecord = {
	id : number
	resourceType : string
	resourceId : number
	title : string
	subtitle : string
	coverUrl : string
	resourceTypeLabel : string
	visitedTime : string
	progressPercent : number
	studySeconds : number
}

export type AppFavoriteRequest = {
	resourceType : string
	resourceId : number
	favorited : boolean
}

export type AppResourceInteraction = {
	resourceType : string
	resourceId : number
	browseCount : number
	favoriteCount : number
	favorited : boolean
}

export type HomeCategory = {
	id : number
	parentId : number
	categoryName : string
	categoryCode : string
	iconUrl : string
	description : string
	sortOrder : number
	status : string
}

export type HomeContent = {
	id : number
	categoryId : number
	contentType : string
	targetId : number
	title : string
	coverUrl : string
	linkUrl : string
	sortOrder : number
	startAt : string
	endAt : string
	status : string
}

export type AppHomeItem = {
	id : number
	contentType : string
	contentTypeLabel : string
	targetId : number
	title : string
	subtitle : string
	summary : string
	coverUrl : string
	linkUrl : string
	sortOrder : number
}

export type AppHomeSection = {
	id : number
	categoryName : string
	categoryCode : string
	iconUrl : string
	description : string
	sortOrder : number
	items : AppHomeItem[]
}

export type AppHomeResponse = {
	sections : AppHomeSection[]
}

export type AvatarUploadUrlResponse = {
	uploadUrl : string
	objectKey : string
	method : string
	headers : UTSJSONObject | null
	formData : UTSJSONObject | null
	publicUrl : string
}

export type AvatarUploadUrlRequest = {
	originalName : string
	contentType : string
	fileSize : number
}

export type AvatarConfirmRequest = {
	objectKey : string
	originalName? : string
}

export type AvatarBinaryUploadConfig = {
	contentType : string
}

const ACCESS_TOKEN_KEY = 'app_auth_access_token'
const TOKEN_TYPE_KEY = 'app_auth_token_type'
const LOGIN_USER_KEY = 'app_auth_login_user'
const CURRENT_USER_KEY = 'app_auth_current_user'
const BIND_TOKEN_KEY = 'app_auth_bind_token'

function getTokenType() : string {
	const tokenType = uni.getStorageSync(TOKEN_TYPE_KEY)
	return typeof tokenType == 'string' ? tokenType : ''
}

function getAccessToken() : string {
	const accessToken = uni.getStorageSync(ACCESS_TOKEN_KEY)
	return typeof accessToken == 'string' ? accessToken : ''
}

function buildAuthorization() : string {
	const tokenType = getTokenType()
	const accessToken = getAccessToken()
	if (accessToken == '') {
		return ''
	}
	if (tokenType != '') {
		return tokenType + ' ' + accessToken
	}
	return 'Bearer ' + accessToken
}

function getBaseOrigin() : string {
	const matches = BASE_URL.match(/^https?:\/\/[^/]+/)
	if (matches != null && matches.length > 0) {
		return matches[0]
	}
	return BASE_URL
}

function normalizeExternalUrl(rawUrl : string) : string {
	if (rawUrl == null || rawUrl == '') {
		return ''
	}

	const baseOrigin = getBaseOrigin()

	if (/^https?:\/\/minio(?::\d+)?($|\/|\?)/.test(rawUrl)) {
		return rawUrl.replace(/^https?:\/\/minio(?::\d+)?/i, baseOrigin)
	}

	if (/^https?:\/\/[^/]*minio[^/]*($|\/|\?)/.test(rawUrl)) {
		return rawUrl.replace(/^https?:\/\/[^/]*minio[^/]*/i, baseOrigin)
	}

	if (/^https?:\/\/minio(?::\d+)?\//.test(rawUrl)) {
		return rawUrl.replace(/^https?:\/\/minio(?::\d+)?/, baseOrigin)
	}

	if (/^https?:\/\/[^/]+\/minio\//.test(rawUrl) && rawUrl.indexOf(baseOrigin) == 0) {
		return rawUrl
	}

	if (rawUrl.startsWith('/')) {
		return baseOrigin + rawUrl
	}

	return rawUrl
}

export function normalizeAppUrl(rawUrl : string) : string {
	const normalized = normalizeExternalUrl(rawUrl)
	if (normalized.indexOf('https://example.com/assets/') == 0 || normalized.indexOf('http://example.com/assets/') == 0) {
		return ''
	}
	return normalized
}

function normalizeUploadUrlResponse(data : UTSJSONObject) : AvatarUploadUrlResponse {
	const rawUploadUrl =
		(data['uploadUrl'] as string) ||
		(data['signedUrl'] as string) ||
		(data['presignedUrl'] as string) ||
		(data['url'] as string) ||
		''
	const uploadUrl = normalizeExternalUrl(rawUploadUrl)
	const objectKey =
		(data['objectKey'] as string) ||
		(data['fileKey'] as string) ||
		(data['key'] as string) ||
		''
	const method =
		(data['method'] as string) ||
		'PUT'
	const headers = data['headers'] as UTSJSONObject | null
	const formData = data['formData'] as UTSJSONObject | null
	const rawPublicUrl =
		(data['publicUrl'] as string) ||
		(data['avatarUrl'] as string) ||
		(data['fileUrl'] as string) ||
		''
	const publicUrl = normalizeExternalUrl(rawPublicUrl)

	return {
		uploadUrl,
		objectKey,
		method,
		headers,
		formData,
		publicUrl
	} as AvatarUploadUrlResponse
}

export function saveLogin(data : LoginResponseData) {
	uni.setStorageSync(ACCESS_TOKEN_KEY, data.accessToken)
	uni.setStorageSync(TOKEN_TYPE_KEY, data.tokenType)
	if (data.user != null) {
		data.user.avatarUrl = normalizeAppUrl(data.user.avatarUrl)
		uni.setStorageSync(LOGIN_USER_KEY, data.user)
	}
}

function saveCurrentUser(user : CurrentUser) {
	user.avatarUrl = normalizeAppUrl(user.avatarUrl)
	uni.setStorageSync(CURRENT_USER_KEY, user)
}

function normalizeCurrentUserData(raw : UTSJSONObject | null) : CurrentUser | null {
	if (raw == null) {
		return null
	}

	return {
		id: typeof raw['id'] == 'number' ? (raw['id'] as number) : 0,
		username: typeof raw['username'] == 'string' ? (raw['username'] as string) : '',
		nickname: typeof raw['nickname'] == 'string' ? (raw['nickname'] as string) : '',
		avatarUrl: normalizeAppUrl(typeof raw['avatarUrl'] == 'string' ? (raw['avatarUrl'] as string) : ''),
		mobile: typeof raw['mobile'] == 'string' ? (raw['mobile'] as string) : '',
		email: typeof raw['email'] == 'string' ? (raw['email'] as string) : '',
		profileCompleted: typeof raw['profileCompleted'] == 'boolean' ? (raw['profileCompleted'] as boolean) : false,
		studentId: typeof raw['studentId'] == 'number' ? (raw['studentId'] as number) : 0,
		certificationStatus: typeof raw['certificationStatus'] == 'string' ? (raw['certificationStatus'] as string) : ''
	} as CurrentUser
}

export function patchCurrentUser(partial : CurrentUserPatch) {
	const cached = getCurrentUserFromStorage()
	if (cached == null) {
		return
	}

	const nextUser : CurrentUser = {
		id: cached.id,
		username: cached.username,
		nickname: partial.nickname != null && partial.nickname != '' ? partial.nickname! : cached.nickname,
		avatarUrl: partial.avatarUrl != null && partial.avatarUrl != '' ? normalizeAppUrl(partial.avatarUrl!) : cached.avatarUrl,
		mobile: partial.mobile != null && partial.mobile != '' ? partial.mobile! : cached.mobile,
		email: partial.email != null && partial.email != '' ? partial.email! : cached.email,
		profileCompleted: partial.profileCompleted != null ? partial.profileCompleted! : cached.profileCompleted,
		studentId: partial.studentId != null ? partial.studentId! : cached.studentId,
		certificationStatus: partial.certificationStatus != null && partial.certificationStatus != '' ? partial.certificationStatus! : cached.certificationStatus
	}

	saveCurrentUser(nextUser)
}

export function mergeProfileIntoCurrentUser(profile : AppProfile) {
	profile.avatarUrl = normalizeAppUrl(profile.avatarUrl)
	const cached = getCurrentUserFromStorage()
	const nextUser : CurrentUser = {
		id: cached != null ? cached.id : (profile.id || 0),
		username: cached != null ? cached.username : (profile.username || ''),
		nickname: profile.nickname || (cached != null ? cached.nickname : ''),
		avatarUrl: profile.avatarUrl || (cached != null ? cached.avatarUrl : ''),
		mobile: profile.mobile || (cached != null ? cached.mobile : ''),
		email: profile.email || (cached != null ? cached.email : ''),
		profileCompleted: profile.profileCompleted || (cached != null ? cached.profileCompleted : false),
		studentId: profile.studentId || (cached != null ? cached.studentId : 0),
		certificationStatus: profile.certificationStatus || (cached != null ? cached.certificationStatus : '')
	}
	saveCurrentUser(nextUser)
}

export function fetchProfile(
	success : (profile : AppProfile) => void,
	fail : (message : string) => void
) {
	request<AppProfile>(
		'/api/v1/app/profile',
		'GET',
		null,
		true,
		false,
		(profile) => {
			profile.avatarUrl = normalizeAppUrl(profile.avatarUrl)
			mergeProfileIntoCurrentUser(profile)
			success(profile)
		},
		(message) => {
			fail(message)
		}
	)
}

export function updateProfile(
	data : AppProfileUpdateRequest,
	success : (profile : AppProfile) => void,
	fail : (message : string) => void
) {
	request<AppProfile>(
		'/api/v1/app/profile',
		'PUT',
		data as UTSJSONObject,
		true,
		false,
		(profile) => {
			profile.avatarUrl = normalizeAppUrl(profile.avatarUrl)
			mergeProfileIntoCurrentUser(profile)
			success(profile)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchCertificationStatus(
	success : (data : AppStudentCertification) => void,
	fail : (message : string) => void
) {
	request<AppStudentCertification>(
		'/api/v1/app/profile/certification',
		'GET',
		null,
		true,
		false,
		(data) => {
			success(data)
		},
		(message) => {
			fail(message)
		}
	)
}

export function submitCertification(
	data : AppStudentCertificationRequest,
	success : (result : AppStudentCertification) => void,
	fail : (message : string) => void
) {
	request<AppStudentCertification>(
		'/api/v1/app/profile/certification',
		'POST',
		data as UTSJSONObject,
		true,
		false,
		(result) => {
			success(result)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchLiveSessions(
	page : number,
	size : number,
	keyword : string,
	success : (pageData : PageResponse<LiveSession>) => void,
	fail : (message : string) => void
) {
	let path = '/api/v1/app/live-sessions?page=' + String(page) + '&size=' + String(size)
	if (keyword != null && keyword.length > 0) {
		path += '&keyword=' + encodeURIComponent(keyword)
	}
	request<PageResponse<LiveSession>>(
		path,
		'GET',
		null,
		true,
		false,
		(pageData) => {
			const records = pageData.records != null ? pageData.records : []
			success({
				records: records.map((item : UTSJSONObject) : LiveSession => {
					return normalizeLiveSession(item)
				}),
				total: pageData.total,
				page: pageData.page,
				size: pageData.size
			} as PageResponse<LiveSession>)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchLiveSessionDetail(
	id : string,
	success : (detail : LiveSession) => void,
	fail : (message : string) => void
) {
	request<LiveSession>(
		'/api/v1/app/live-sessions/' + id,
		'GET',
		null,
		true,
		false,
		(detail) => {
			success(normalizeLiveSession(detail))
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchCourses(
	page : number,
	size : number,
	keyword : string,
	success : (pageData : PageResponse<Course>) => void,
	fail : (message : string) => void
) {
	let path = '/api/v1/app/learning/courses?page=' + String(page) + '&size=' + String(size)
	if (keyword != null && keyword.length > 0) {
		path += '&keyword=' + encodeURIComponent(keyword)
	}
	request<PageResponse<Course>>(
		path,
		'GET',
		null,
		true,
		false,
		(pageData) => {
			success(pageData)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchCourseDetail(
	id : string,
	success : (detail : Course) => void,
	fail : (message : string) => void
) {
	request<Course>(
		'/api/v1/app/learning/courses/' + id,
		'GET',
		null,
		true,
		false,
		(detail) => {
			success(detail)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchAudioDetail(
	id : string,
	success : (detail : AppPodcast) => void,
	fail : (message : string) => void
) {
	request<AppPodcast>(
		'/api/v1/app/learning/podcasts/' + id,
		'GET',
		null,
		true,
		false,
		(detail) => {
			detail.coverUrl = normalizeAppUrl(detail.coverUrl)
			success(detail)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchPodcastsPage(
	page : number,
	size : number,
	keyword : string,
	success : (pageData : PageResponse<AppPodcast>) => void,
	fail : (message : string) => void
) {
	let path = '/api/v1/app/learning/podcasts?page=' + String(page) + '&size=' + String(size)
	if (keyword != null && keyword.length > 0) {
		path += '&keyword=' + encodeURIComponent(keyword)
	}
	request<PageResponse<AppPodcast>>(
		path,
		'GET',
		null,
		true,
		false,
		(pageData) => {
			const records = pageData.records != null ? pageData.records : []
			success({
				records: records.map((item : AppPodcast) : AppPodcast => {
					return {
						...item,
						coverUrl: normalizeAppUrl(item.coverUrl)
					}
				}),
				total: pageData.total,
				page: pageData.page,
				size: pageData.size
			} as PageResponse<AppPodcast>)
		},
		(message) => {
			fail(message)
		}
	)
}

export type ExpertExperience = {
	id : number
	expertId : number
	experienceType : string
	title : string
	description : string
	startDate : string
	endDate : string
	sortOrder : number
}

export type ExpertDetail = {
	id : number
	realName : string
	gender? : string
	birthDate? : string
	mobile? : string
	avatarUrl : string
	coverUrl? : string
	title : string
	organization : string
	specialty : string
	introduction : string
	consultationNotice : string
	sortOrder : number
	categoryIds : number[]
	experiences : ExpertExperience[]
}

export type AppQaAnswer = {
	id : number
	questionId : number
	adminId : number
	expertId : number
	content : string
	answeredAt : string
}

export type AppQaQuestion = {
	id : number
	expertCategoryId : number
	expertId : number
	title : string
	content : string
	status : string
	statusCode : string
	statusLabel : string
	answers : AppQaAnswer[]
}

export type AppQaQuestionRequest = {
	expertCategoryId? : number
	expertId? : number
	title : string
	content : string
}

function normalizeExpertExperience(raw : UTSJSONObject) : ExpertExperience {
	return {
		id: readNumberField(raw, 'id'),
		expertId: readNumberField(raw, 'expertId'),
		experienceType: readStringField(raw, 'experienceType'),
		title: readStringField(raw, 'title'),
		description: readStringField(raw, 'description'),
		startDate: readStringField(raw, 'startDate'),
		endDate: readStringField(raw, 'endDate'),
		sortOrder: readNumberField(raw, 'sortOrder')
	}
}

function normalizeExpertDetail(raw : UTSJSONObject) : ExpertDetail {
	const categoryIdsValue = raw['categoryIds']
	const rawCategoryIds = categoryIdsValue != null && categoryIdsValue instanceof Array ? (categoryIdsValue as number[]) : []
	const experiencesValue = raw['experiences']
	const rawExperiences = experiencesValue != null && experiencesValue instanceof Array ? (experiencesValue as UTSJSONObject[]) : []

	return {
		id: readNumberField(raw, 'id'),
		realName: readStringField(raw, 'realName'),
		gender: readStringField(raw, 'gender'),
		birthDate: readStringField(raw, 'birthDate'),
		mobile: readStringField(raw, 'mobile'),
		avatarUrl: readStringField(raw, 'avatarUrl'),
		coverUrl: readStringField(raw, 'coverUrl'),
		title: readStringField(raw, 'title'),
		organization: readStringField(raw, 'organization'),
		specialty: readStringField(raw, 'specialty'),
		introduction: readStringField(raw, 'introduction'),
		consultationNotice: readStringField(raw, 'consultationNotice'),
		sortOrder: readNumberField(raw, 'sortOrder'),
		categoryIds: rawCategoryIds,
		experiences: rawExperiences.map((item : UTSJSONObject) : ExpertExperience => {
			return normalizeExpertExperience(item)
		})
	}
}

function normalizeQaAnswer(raw : UTSJSONObject) : AppQaAnswer {
	return {
		id: readNumberField(raw, 'id'),
		questionId: readNumberField(raw, 'questionId'),
		adminId: readNumberField(raw, 'adminId'),
		expertId: readNumberField(raw, 'expertId'),
		content: readStringField(raw, 'content'),
		answeredAt: readStringField(raw, 'answeredAt')
	}
}

function normalizeQaQuestion(raw : UTSJSONObject) : AppQaQuestion {
	const answersValue = raw['answers']
	const rawAnswers = answersValue != null && answersValue instanceof Array ? (answersValue as UTSJSONObject[]) : []

	return {
		id: readNumberField(raw, 'id'),
		expertCategoryId: readNumberField(raw, 'expertCategoryId'),
		expertId: readNumberField(raw, 'expertId'),
		title: readStringField(raw, 'title'),
		content: readStringField(raw, 'content'),
		status: readStringField(raw, 'status'),
		statusCode: readStringField(raw, 'statusCode'),
		statusLabel: readStringField(raw, 'statusLabel'),
		answers: rawAnswers.map((item : UTSJSONObject) : AppQaAnswer => {
			return normalizeQaAnswer(item)
		})
	}
}

export function fetchExpertDetail(
	id : string,
	success : (detail : ExpertDetail) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/experts/' + id,
		'GET',
		null,
		true,
		false,
		(detail) => {
			success(normalizeExpertDetail(detail))
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchExperts(
	page : number,
	size : number,
	keyword : string,
	categoryId : number,
	success : (pageData : PageResponse<ExpertDetail>) => void,
	fail : (message : string) => void
) {
	let path = '/api/v1/app/experts?page=' + String(page) + '&size=' + String(size)
	if (keyword != null && keyword.length > 0) {
		path += '&keyword=' + encodeURIComponent(keyword)
	}
	if (categoryId > 0) {
		path += '&categoryId=' + String(categoryId)
	}

	request<UTSJSONObject>(
		path,
		'GET',
		null,
		true,
		false,
		(pageData) => {
			const recordsValue = pageData['records']
			const records = recordsValue != null && recordsValue instanceof Array ? (recordsValue as UTSJSONObject[]) : []
			success({
				records: records.map((item : UTSJSONObject) : ExpertDetail => {
					return normalizeExpertDetail(item)
				}),
				total: readNumberField(pageData, 'total'),
				page: readNumberField(pageData, 'page'),
				size: readNumberField(pageData, 'size')
			})
		},
		(message) => {
			fail(message)
		}
	)
}

export function createQaQuestion(
	data : AppQaQuestionRequest,
	success : (result : AppQaQuestion) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/interaction/qa/questions',
		'POST',
		data as UTSJSONObject,
		true,
		false,
		(result) => {
			success(normalizeQaQuestion(result))
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchQaQuestions(
	page : number,
	size : number,
	success : (pageData : PageResponse<AppQaQuestion>) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/interaction/qa/questions?page=' + String(page) + '&size=' + String(size),
		'GET',
		null,
		true,
		true,
		(pageData) => {
			if (pageData == null) {
				success({
					records: [],
					total: 0,
					page,
					size
				})
				return
			}
			const recordsValue = pageData['records']
			const records = recordsValue != null && recordsValue instanceof Array ? (recordsValue as UTSJSONObject[]) : []
			success({
				records: records.map((item : UTSJSONObject) : AppQaQuestion => {
					return normalizeQaQuestion(item)
				}),
				total: readNumberField(pageData, 'total') > 0 ? readNumberField(pageData, 'total') : records.length,
				page: readNumberField(pageData, 'page') > 0 ? readNumberField(pageData, 'page') : page,
				size: readNumberField(pageData, 'size') > 0 ? readNumberField(pageData, 'size') : size
			})
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchQaQuestionDetail(
	id : string,
	success : (detail : AppQaQuestion) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/interaction/qa/questions/' + id,
		'GET',
		null,
		true,
		false,
		(detail) => {
			success(normalizeQaQuestion(detail))
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchTopics(
	success : (pageData : PageResponse<Topic>) => void,
	fail : (message : string) => void
) {
	request<PageResponse<Topic>>(
		'/api/v1/app/learning/topics?page=1&size=20',
		'GET',
		null,
		true,
		false,
		(pageData) => {
			success(pageData)
		},
		(message) => {
			fail(message)
		}
	)
}

function readStringField(raw : UTSJSONObject, key : string) : string {
	const value = raw[key]
	return typeof value == 'string' ? (value as string) : ''
}

function readNumberField(raw : UTSJSONObject, key : string) : number {
	const value = raw[key]
	return typeof value == 'number' ? (value as number) : 0
}

function readLooseNumberField(raw : UTSJSONObject, key : string) : number {
	const value = raw[key]
	if (typeof value == 'number') {
		return value as number
	}
	if (typeof value == 'string') {
		const text = value as string
		if (text.length > 0) {
			return Number(text)
		}
	}
	return 0
}

function readBooleanField(raw : UTSJSONObject, key : string) : boolean {
	const value = raw[key]
	return typeof value == 'boolean' ? (value as boolean) : false
}

function readStringArrayField(raw : UTSJSONObject, key : string) : string[] {
	const value = raw[key]
	if (value == null || !(value instanceof Array)) {
		return []
	}
	return (value as Array<any>).filter((item : any) : boolean => typeof item == 'string').map((item : any) : string => item as string)
}

function normalizeTopicSectionResource(raw : UTSJSONObject) : TopicSectionResource {
	const resourceId = readLooseNumberField(raw, 'resourceId')
	const itemId = readLooseNumberField(raw, 'itemId')
	const entryId = readLooseNumberField(raw, 'entryId')
	const knowledgeId = readLooseNumberField(raw, 'knowledgeId')
	const contentId = readLooseNumberField(raw, 'contentId')
	const id = readLooseNumberField(raw, 'id')
	const resourceValue = raw['resource']
	const nestedResource = resourceValue != null && resourceValue instanceof UTSJSONObject ? (resourceValue as UTSJSONObject) : null
	const nestedId = nestedResource != null ? readLooseNumberField(nestedResource, 'id') : 0
	const nestedEntryId = nestedResource != null ? readLooseNumberField(nestedResource, 'entryId') : 0
	const nestedKnowledgeId = nestedResource != null ? readLooseNumberField(nestedResource, 'knowledgeId') : 0
	const nestedContentId = nestedResource != null ? readLooseNumberField(nestedResource, 'contentId') : 0
	const resolvedResourceType = readStringField(raw, 'resourceType').length > 0
		? readStringField(raw, 'resourceType')
		: (readStringField(raw, 'itemType').length > 0
			? readStringField(raw, 'itemType')
			: (nestedResource != null ? readStringField(nestedResource, 'resourceType') : ''))
	const resolvedTitle = readStringField(raw, 'title').length > 0
		? readStringField(raw, 'title')
		: (nestedResource != null ? readStringField(nestedResource, 'title') : '')
	const resolvedSubtitle = readStringField(raw, 'subtitle').length > 0
		? readStringField(raw, 'subtitle')
		: (nestedResource != null ? readStringField(nestedResource, 'subtitle') : '')
	const resolvedSummary = readStringField(raw, 'summary').length > 0
		? readStringField(raw, 'summary')
		: (nestedResource != null ? readStringField(nestedResource, 'summary') : '')
	const resolvedCoverUrl = readStringField(raw, 'coverUrl').length > 0
		? readStringField(raw, 'coverUrl')
		: (nestedResource != null ? readStringField(nestedResource, 'coverUrl') : '')
	return {
		resourceType: resolvedResourceType,
		resourceId: resourceId > 0
			? resourceId
			: (itemId > 0
				? itemId
				: (entryId > 0
					? entryId
					: (knowledgeId > 0
						? knowledgeId
						: (contentId > 0
							? contentId
							: (nestedEntryId > 0
								? nestedEntryId
								: (nestedKnowledgeId > 0
									? nestedKnowledgeId
									: (nestedContentId > 0
										? nestedContentId
										: (nestedId > 0 ? nestedId : id)))))))),
		title: resolvedTitle,
		subtitle: resolvedSubtitle,
		summary: resolvedSummary,
		coverUrl: normalizeAppUrl(resolvedCoverUrl),
		browseCount: readNumberField(raw, 'browseCount') > 0 ? readNumberField(raw, 'browseCount') : (nestedResource != null ? readNumberField(nestedResource, 'browseCount') : 0),
		favoriteCount: readNumberField(raw, 'favoriteCount') > 0 ? readNumberField(raw, 'favoriteCount') : (nestedResource != null ? readNumberField(nestedResource, 'favoriteCount') : 0),
		favorited: readBooleanField(raw, 'favorited') || (nestedResource != null ? readBooleanField(nestedResource, 'favorited') : false),
		progressPercent: readNumberField(raw, 'progressPercent') > 0 ? readNumberField(raw, 'progressPercent') : (nestedResource != null ? readNumberField(nestedResource, 'progressPercent') : 0)
	}
}

function normalizeTopicSection(raw : UTSJSONObject) : TopicSection {
	const previewValue = raw['previewItems'] != null ? raw['previewItems'] : raw['items']
	const previewItems = previewValue != null && previewValue instanceof Array ? (previewValue as UTSJSONObject[]) : []
	return {
		sectionType: readStringField(raw, 'sectionType'),
		sectionLabel: readStringField(raw, 'sectionLabel'),
		total: readNumberField(raw, 'total'),
		hasMore: readBooleanField(raw, 'hasMore'),
		previewItems: previewItems.map((item : UTSJSONObject) : TopicSectionResource => {
			return normalizeTopicSectionResource(item)
		})
	}
}

function normalizeTopicCard(raw : UTSJSONObject) : TopicCard {
	return {
		id: readNumberField(raw, 'id'),
		title: readStringField(raw, 'title'),
		summary: readStringField(raw, 'summary'),
		learningRequirements: readStringField(raw, 'learningRequirements'),
		coverUrl: normalizeAppUrl(readStringField(raw, 'coverUrl')),
		tags: readStringArrayField(raw, 'tags'),
		viewCount: readNumberField(raw, 'viewCount'),
		publishedAt: readStringField(raw, 'publishedAt'),
		favoriteCount: readNumberField(raw, 'favoriteCount'),
		favorited: readBooleanField(raw, 'favorited')
	}
}

function normalizeTopicDetail(raw : UTSJSONObject) : TopicDetail {
	const sectionsValue = raw['sections']
	const sections = sectionsValue != null && sectionsValue instanceof Array ? (sectionsValue as UTSJSONObject[]) : []
	return {
		id: readNumberField(raw, 'id'),
		title: readStringField(raw, 'title'),
		summary: readStringField(raw, 'summary'),
		learningRequirements: readStringField(raw, 'learningRequirements'),
		coverUrl: normalizeAppUrl(readStringField(raw, 'coverUrl')),
		tags: readStringArrayField(raw, 'tags'),
		viewCount: readNumberField(raw, 'viewCount'),
		publishedAt: readStringField(raw, 'publishedAt'),
		favoriteCount: readNumberField(raw, 'favoriteCount'),
		favorited: readBooleanField(raw, 'favorited'),
		sections: sections.map((item : UTSJSONObject) : TopicSection => {
			return normalizeTopicSection(item)
		})
	}
}

function normalizeArticle(raw : UTSJSONObject) : Article {
	return {
		id: readNumberField(raw, 'id'),
		title: readStringField(raw, 'title'),
		summary: readStringField(raw, 'summary'),
		coverUrl: normalizeAppUrl(readStringField(raw, 'coverUrl')),
		content: readStringField(raw, 'content'),
		authorName: readStringField(raw, 'authorName'),
		source: readStringField(raw, 'source'),
		tags: readStringArrayField(raw, 'tags'),
		viewCount: readNumberField(raw, 'viewCount'),
		favoriteCount: readNumberField(raw, 'favoriteCount'),
		favorited: readBooleanField(raw, 'favorited'),
		publishedAt: readStringField(raw, 'publishedAt')
	}
}

function normalizeLiveSessionVideo(raw : UTSJSONObject) : LiveSessionVideo {
	return {
		id: typeof raw['id'] == 'number' ? (raw['id'] as number) : 0,
		liveSessionId: typeof raw['liveSessionId'] == 'number' ? (raw['liveSessionId'] as number) : 0,
		title: typeof raw['title'] == 'string' ? (raw['title'] as string) : '',
		videoUrl: normalizeAppUrl(typeof raw['videoUrl'] == 'string' ? (raw['videoUrl'] as string) : ''),
		durationSeconds: typeof raw['durationSeconds'] == 'number' ? (raw['durationSeconds'] as number) : 0,
		sortOrder: typeof raw['sortOrder'] == 'number' ? (raw['sortOrder'] as number) : 0,
		status: typeof raw['status'] == 'string' ? (raw['status'] as string) : ''
	} as LiveSessionVideo
}

function normalizeLiveSession(raw : UTSJSONObject) : LiveSession {
	const videoRaw = raw['videos']
	const videos = Array.isArray(videoRaw)
		? (videoRaw as UTSJSONObject[]).map((item : UTSJSONObject) : LiveSessionVideo => {
			return normalizeLiveSessionVideo(item)
		})
		: []

	const tagRaw = raw['tags']
	const tags = Array.isArray(tagRaw)
		? (tagRaw as string[]).filter((item : string) : boolean => {
			return item != null && item.length > 0
		})
		: []

	return {
		id: typeof raw['id'] == 'number' ? (raw['id'] as number) : 0,
		title: typeof raw['title'] == 'string' ? (raw['title'] as string) : '',
		coverUrl: normalizeAppUrl(typeof raw['coverUrl'] == 'string' ? (raw['coverUrl'] as string) : ''),
		anchorName: typeof raw['anchorName'] == 'string' ? (raw['anchorName'] as string) : '',
		speakerName: typeof raw['speakerName'] == 'string' ? (raw['speakerName'] as string) : '',
		tags,
		liveUrl: normalizeAppUrl(typeof raw['liveUrl'] == 'string' ? (raw['liveUrl'] as string) : ''),
		playbackUrl: normalizeAppUrl(typeof raw['playbackUrl'] == 'string' ? (raw['playbackUrl'] as string) : ''),
		startAt: typeof raw['startAt'] == 'string' ? (raw['startAt'] as string) : '',
		endAt: typeof raw['endAt'] == 'string' ? (raw['endAt'] as string) : '',
		reviewStatus: typeof raw['reviewStatus'] == 'string' ? (raw['reviewStatus'] as string) : '',
		liveStatus: typeof raw['liveStatus'] == 'string' ? (raw['liveStatus'] as string) : '',
		browseCount: typeof raw['browseCount'] == 'number' ? (raw['browseCount'] as number) : 0,
		favoriteCount: typeof raw['favoriteCount'] == 'number' ? (raw['favoriteCount'] as number) : 0,
		favorited: typeof raw['favorited'] == 'boolean' ? (raw['favorited'] as boolean) : false,
		videos
	} as LiveSession
}

function normalizeLearningHistoryRecord(raw : UTSJSONObject) : LearningHistoryRecord {
	const resourceType = readStringField(raw, 'resourceType')
	const title = readStringField(raw, 'title').length > 0
		? readStringField(raw, 'title')
		: (readStringField(raw, 'bookName').length > 0
			? readStringField(raw, 'bookName')
			: (readStringField(raw, 'courseName').length > 0
				? readStringField(raw, 'courseName')
				: readStringField(raw, 'topicName')))
	const subtitle = readStringField(raw, 'subtitle').length > 0
		? readStringField(raw, 'subtitle')
		: (readStringField(raw, 'author').length > 0
			? readStringField(raw, 'author')
			: (readStringField(raw, 'lecturerName').length > 0
				? readStringField(raw, 'lecturerName')
				: readStringField(raw, 'source')))
	const visitedTime = readStringField(raw, 'visitedTime').length > 0
		? readStringField(raw, 'visitedTime')
		: readStringField(raw, 'occurredAt')
	const resourceTypeLabel = readStringField(raw, 'resourceTypeLabel').length > 0
		? readStringField(raw, 'resourceTypeLabel')
		: resourceType
	return {
		id: readNumberField(raw, 'id'),
		resourceType,
		resourceId: readNumberField(raw, 'resourceId'),
		title,
		subtitle,
		coverUrl: normalizeAppUrl(readStringField(raw, 'coverUrl')),
		resourceTypeLabel,
		visitedTime,
		progressPercent: readNumberField(raw, 'progressPercent'),
		studySeconds: readNumberField(raw, 'studySeconds')
	}
}

function mapLearningHistoryPage(
	pageData : UTSJSONObject,
	page : number,
	size : number
) : PageResponse<LearningHistoryRecord> {
	const recordsValue = pageData['records']
	const records = recordsValue != null && recordsValue instanceof Array ? (recordsValue as UTSJSONObject[]) : []
	return {
		records: records.map((item : UTSJSONObject) : LearningHistoryRecord => {
			return normalizeLearningHistoryRecord(item)
		}),
		total: readNumberField(pageData, 'total'),
		page: readNumberField(pageData, 'page') > 0 ? readNumberField(pageData, 'page') : page,
		size: readNumberField(pageData, 'size') > 0 ? readNumberField(pageData, 'size') : size
	}
}

function normalizeExamQuestionOption(raw : UTSJSONObject) : ExamQuestionOption {
	const optionKey = readStringField(raw, 'optionKey').length > 0
		? readStringField(raw, 'optionKey')
		: (readStringField(raw, 'key').length > 0
			? readStringField(raw, 'key')
			: readStringField(raw, 'label'))
	const optionContent = readStringField(raw, 'optionContent').length > 0
		? readStringField(raw, 'optionContent')
		: (readStringField(raw, 'content').length > 0
			? readStringField(raw, 'content')
			: (readStringField(raw, 'optionText').length > 0
				? readStringField(raw, 'optionText')
				: readStringField(raw, 'label')))
	return {
		id: readNumberField(raw, 'id'),
		optionKey,
		optionContent,
		sortOrder: readNumberField(raw, 'sortOrder') > 0 ? readNumberField(raw, 'sortOrder') : readNumberField(raw, 'orderNum')
	}
}

function readObjectArrayField(raw : UTSJSONObject, key : string) : UTSJSONObject[] {
	const value = raw[key]
	if (value == null || !(value instanceof Array)) {
		return []
	}
	return (value as Array<any>).filter((item : any) : boolean => item != null && typeof item == 'object').map((item : any) : UTSJSONObject => item as UTSJSONObject)
}

function normalizeExamQuestionTypeValue(value : any) : string {
	if (typeof value == 'number') {
		const typeCode = value as number
		if (typeCode == 1) {
			return 'SINGLE_CHOICE'
		}
		if (typeCode == 2) {
			return 'MULTIPLE_CHOICE'
		}
		if (typeCode == 3) {
			return 'TRUE_FALSE'
		}
		if (typeCode == 4) {
			return 'SHORT_ANSWER'
		}
		return ''
	}
	if (typeof value == 'string') {
		const text = (value as string).toUpperCase()
		if (text == '1' || text == 'SINGLE_CHOICE' || text == 'SINGLE' || text == 'RADIO') {
			return 'SINGLE_CHOICE'
		}
		if (text == '2' || text == 'MULTIPLE_CHOICE' || text == 'MULTIPLE' || text == 'CHECKBOX') {
			return 'MULTIPLE_CHOICE'
		}
		if (text == '3' || text == 'TRUE_FALSE' || text == 'JUDGE' || text == 'BOOLEAN') {
			return 'TRUE_FALSE'
		}
		if (text == '4' || text == 'SHORT_ANSWER' || text == 'TEXT' || text == 'ESSAY' || text == 'FILL_BLANK') {
			return 'SHORT_ANSWER'
		}
	}
	return ''
}

function readExamQuestionType(raw : UTSJSONObject, key : string) : string {
	return normalizeExamQuestionTypeValue(raw[key])
}

function normalizeExamQuestion(raw : UTSJSONObject) : ExamQuestion {
	const options = readObjectArrayField(raw, 'options')
	const questionRaw = raw['question'] != null ? (raw['question'] as UTSJSONObject) : raw
	const questionId = readNumberField(raw, 'questionId') > 0 ? readNumberField(raw, 'questionId') : readNumberField(questionRaw, 'id')
	const score = readNumberField(raw, 'score') > 0 ? readNumberField(raw, 'score') : readNumberField(questionRaw, 'score')
	const sortOrder = readNumberField(raw, 'sortOrder')
	const nestedOptions = readObjectArrayField(questionRaw, 'options')
	const questionOptions = readObjectArrayField(questionRaw, 'questionOptions')
	const optionList = readObjectArrayField(questionRaw, 'optionList')
	const answerOptions = readObjectArrayField(questionRaw, 'answers')
	const normalizedOptionsSource = options.length > 0
		? options
		: (nestedOptions.length > 0
			? nestedOptions
			: (questionOptions.length > 0
				? questionOptions
				: (optionList.length > 0 ? optionList : answerOptions)))
	const questionType = readExamQuestionType(questionRaw, 'questionType').length > 0
		? readExamQuestionType(questionRaw, 'questionType')
		: readExamQuestionType(raw, 'questionType')
	return {
		questionId,
		questionType,
		title: readStringField(questionRaw, 'title'),
		analysis: readStringField(questionRaw, 'analysis'),
		difficulty: readStringField(questionRaw, 'difficulty'),
		score,
		sortOrder,
		options: normalizedOptionsSource.map((item : UTSJSONObject) : ExamQuestionOption => {
			return normalizeExamQuestionOption(item)
		})
	}
}

function normalizeExamPaper(raw : UTSJSONObject) : ExamPaper {
	const questionsValue = raw['questions']
	const questions = questionsValue != null && questionsValue instanceof Array ? (questionsValue as UTSJSONObject[]) : []
	const questionCountValue = readNumberField(raw, 'questionCount')
	return {
		id: readNumberField(raw, 'id'),
		paperName: readStringField(raw, 'paperName'),
		description: readStringField(raw, 'description'),
		totalScore: readNumberField(raw, 'totalScore'),
		passScore: readNumberField(raw, 'passScore'),
		durationMinutes: readNumberField(raw, 'durationMinutes'),
		status: readStringField(raw, 'status'),
		questionCount: questionCountValue > 0 ? questionCountValue : questions.length,
		questions: questions.map((item : UTSJSONObject) : ExamQuestion => {
			return normalizeExamQuestion(item)
		})
	}
}

function normalizeExamAnswerResult(raw : UTSJSONObject) : ExamAnswerResult {
	const optionsValue = raw['options']
	const options = optionsValue != null && optionsValue instanceof Array ? (optionsValue as UTSJSONObject[]) : []
	return {
		questionId: readNumberField(raw, 'questionId'),
		questionType: readExamQuestionType(raw, 'questionType'),
		title: readStringField(raw, 'title'),
		answerContent: readStringField(raw, 'answerContent'),
		correctAnswer: readStringField(raw, 'correctAnswer'),
		analysis: readStringField(raw, 'analysis'),
		score: readNumberField(raw, 'score'),
		correct: readNumberField(raw, 'correct'),
		options: options.map((item : UTSJSONObject) : ExamQuestionOption => {
			return normalizeExamQuestionOption(item)
		})
	}
}

function normalizeExamRecord(raw : UTSJSONObject) : ExamRecord {
	const answersValue = raw['answers']
	const answers = answersValue != null && answersValue instanceof Array ? (answersValue as UTSJSONObject[]) : []
	return {
		id: readNumberField(raw, 'id'),
		paperId: readNumberField(raw, 'paperId'),
		paperName: readStringField(raw, 'paperName'),
		score: readNumberField(raw, 'score'),
		passed: readNumberField(raw, 'passed'),
		totalScore: readNumberField(raw, 'totalScore'),
		passScore: readNumberField(raw, 'passScore'),
		submittedAt: readStringField(raw, 'submittedAt'),
		answers: answers.map((item : UTSJSONObject) : ExamAnswerResult => {
			return normalizeExamAnswerResult(item)
		})
	}
}

function normalizeKnowledgeCategory(raw : UTSJSONObject) : KnowledgeCategory {
	const childrenValue = raw['children']
	const rawChildren = childrenValue != null && childrenValue instanceof Array ? (childrenValue as UTSJSONObject[]) : []
	const children = rawChildren.map((item : UTSJSONObject) : KnowledgeCategory => {
		return normalizeKnowledgeCategory(item)
	})

	return {
		id: readNumberField(raw, 'id'),
		parentId: readNumberField(raw, 'parentId'),
		categoryName: readStringField(raw, 'categoryName'),
		categoryCode: readStringField(raw, 'categoryCode'),
		description: readStringField(raw, 'description'),
		sortOrder: readNumberField(raw, 'sortOrder'),
		children
	} as KnowledgeCategory
}

function normalizeKnowledgeEntry(raw : UTSJSONObject) : KnowledgeEntry {
	return {
		id: readNumberField(raw, 'id'),
		categoryId: readNumberField(raw, 'categoryId'),
		title: readStringField(raw, 'title'),
		summary: readStringField(raw, 'summary'),
		categoryName: readStringField(raw, 'categoryName'),
		categoryCode: readStringField(raw, 'categoryCode'),
		coverUrl: normalizeAppUrl(readStringField(raw, 'coverUrl')),
		content: readStringField(raw, 'content'),
		keywords: readStringField(raw, 'keywords'),
		source: readStringField(raw, 'source'),
		author: readStringField(raw, 'author'),
		publisher: readStringField(raw, 'publisher'),
		totalPages: readNumberField(raw, 'totalPages'),
		publishedAt: readStringField(raw, 'publishedAt'),
		viewCount: readNumberField(raw, 'viewCount'),
		sortOrder: readNumberField(raw, 'sortOrder')
	} as KnowledgeEntry
}

function normalizeBookChapter(raw : UTSJSONObject) : AppBookChapter {
	return {
		id: readNumberField(raw, 'id'),
		bookId: readNumberField(raw, 'bookId'),
		parentId: readNumberField(raw, 'parentId'),
		chapterTitle: readStringField(raw, 'chapterTitle'),
		content: readStringField(raw, 'content'),
		startPage: readNumberField(raw, 'startPage'),
		pageCount: readNumberField(raw, 'pageCount'),
		paperId: readNumberField(raw, 'paperId'),
		sortOrder: readNumberField(raw, 'sortOrder')
	} as AppBookChapter
}

function normalizeBookCategory(raw : UTSJSONObject) : AppBookCategory {
	return {
		id: readNumberField(raw, 'id'),
		parentId: readNumberField(raw, 'parentId'),
		categoryName: readStringField(raw, 'categoryName'),
		sortOrder: readNumberField(raw, 'sortOrder')
	} as AppBookCategory
}

function normalizeBook(raw : UTSJSONObject) : AppBook {
	const chaptersValue = raw['chapters']
	const chapters = chaptersValue != null && chaptersValue instanceof Array ? (chaptersValue as UTSJSONObject[]) : []
	return {
		id: readNumberField(raw, 'id'),
		categoryId: readNumberField(raw, 'categoryId'),
		bookName: readStringField(raw, 'bookName').length > 0 ? readStringField(raw, 'bookName') : readStringField(raw, 'title'),
		author: readStringField(raw, 'author'),
		publisher: readStringField(raw, 'publisher'),
		coverUrl: normalizeAppUrl(readStringField(raw, 'coverUrl')),
		introduction: readStringField(raw, 'introduction').length > 0 ? readStringField(raw, 'introduction') : readStringField(raw, 'summary'),
		totalPages: readNumberField(raw, 'totalPages'),
		paperId: readNumberField(raw, 'paperId'),
		publishedAt: readStringField(raw, 'publishedAt'),
		browseCount: readNumberField(raw, 'browseCount'),
		favoriteCount: readNumberField(raw, 'favoriteCount'),
		favorited: readBooleanField(raw, 'favorited'),
		progressPercent: readNumberField(raw, 'progressPercent'),
		studySeconds: readNumberField(raw, 'studySeconds'),
		chapters: chapters.map((item : UTSJSONObject) : AppBookChapter => {
			return normalizeBookChapter(item)
		})
	} as AppBook
}

export function fetchKnowledgeCategoryTree(
	success : (categories : KnowledgeCategory[]) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject[]>(
		'/api/v1/app/knowledge/categories/tree',
		'GET',
		null,
		true,
		false,
		(categories) => {
			const categoryList = categories != null && categories instanceof Array ? categories : []
			const normalized = categoryList.map((item : UTSJSONObject) : KnowledgeCategory => {
				return normalizeKnowledgeCategory(item)
			})
			success(normalized)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchKnowledgeEntries(
	page : number,
	size : number,
	keyword : string,
	categoryId : number,
	success : (pageData : PageResponse<KnowledgeEntry>) => void,
	fail : (message : string) => void
) {
	let path = '/api/v1/app/knowledge/entries?page=' + String(page) + '&size=' + String(size)
	if (keyword != null && keyword.length > 0) {
		path += '&keyword=' + encodeURIComponent(keyword)
	}
	if (categoryId > 0) {
		path += '&categoryId=' + String(categoryId)
	}

	request<UTSJSONObject>(
		path,
		'GET',
		null,
		true,
		false,
		(pageData) => {
			const recordsValue = pageData['records']
			const records = recordsValue != null && recordsValue instanceof Array ? (recordsValue as UTSJSONObject[]) : []
			const normalizedRecords = records.map((item : UTSJSONObject) : KnowledgeEntry => {
				return normalizeKnowledgeEntry(item)
			})
			success({
				records: normalizedRecords,
				total: readNumberField(pageData, 'total') > 0 ? readNumberField(pageData, 'total') : normalizedRecords.length,
				page: readNumberField(pageData, 'page') > 0 ? readNumberField(pageData, 'page') : page,
				size: readNumberField(pageData, 'size') > 0 ? readNumberField(pageData, 'size') : size
			} as PageResponse<KnowledgeEntry>)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchKnowledgeEntryDetail(
	id : string,
	success : (detail : KnowledgeEntry) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/knowledge/entries/' + id,
		'GET',
		null,
		true,
		false,
		(detail) => {
			success(normalizeKnowledgeEntry(detail))
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchBookDetail(
	id : string,
	success : (detail : AppBook) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/learning/books/' + id,
		'GET',
		null,
		true,
		false,
		(detail) => {
			success(normalizeBook(detail))
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchBookCategories(
	page : number,
	size : number,
	keyword : string,
	parentId : number,
	success : (pageData : PageResponse<AppBookCategory>) => void,
	fail : (message : string) => void
) {
	let path = '/api/v1/app/learning/book-categories?page=' + String(page) + '&size=' + String(size)
	if (keyword != null && keyword.length > 0) {
		path += '&keyword=' + encodeURIComponent(keyword)
	}
	if (parentId > 0) {
		path += '&parentId=' + String(parentId)
	}

	request<UTSJSONObject>(
		path,
		'GET',
		null,
		true,
		false,
		(pageData) => {
			const recordsValue = pageData['records']
			const records = recordsValue != null && recordsValue instanceof Array ? (recordsValue as UTSJSONObject[]) : []
			const normalizedRecords = records.map((item : UTSJSONObject) : AppBookCategory => {
				return normalizeBookCategory(item)
			})
			success({
				records: normalizedRecords,
				total: readNumberField(pageData, 'total') > 0 ? readNumberField(pageData, 'total') : normalizedRecords.length,
				page: readNumberField(pageData, 'page') > 0 ? readNumberField(pageData, 'page') : page,
				size: readNumberField(pageData, 'size') > 0 ? readNumberField(pageData, 'size') : size
			} as PageResponse<AppBookCategory>)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchBooks(
	page : number,
	size : number,
	keyword : string,
	categoryId : number,
	success : (pageData : PageResponse<AppBook>) => void,
	fail : (message : string) => void
) {
	let path = '/api/v1/app/learning/books?page=' + String(page) + '&size=' + String(size)
	if (keyword != null && keyword.length > 0) {
		path += '&keyword=' + encodeURIComponent(keyword)
	}
	if (categoryId > 0) {
		path += '&categoryId=' + String(categoryId)
	}

	request<UTSJSONObject>(
		path,
		'GET',
		null,
		true,
		false,
		(pageData) => {
			const recordsValue = pageData['records']
			const records = recordsValue != null && recordsValue instanceof Array ? (recordsValue as UTSJSONObject[]) : []
			const normalizedRecords = records.map((item : UTSJSONObject) : AppBook => {
				return normalizeBook(item)
			})
			success({
				records: normalizedRecords,
				total: readNumberField(pageData, 'total') > 0 ? readNumberField(pageData, 'total') : normalizedRecords.length,
				page: readNumberField(pageData, 'page') > 0 ? readNumberField(pageData, 'page') : page,
				size: readNumberField(pageData, 'size') > 0 ? readNumberField(pageData, 'size') : size
			} as PageResponse<AppBook>)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchBookChapterDetail(
	bookId : string,
	chapterId : string,
	success : (detail : AppBookChapter) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/learning/books/' + bookId + '/chapters/' + chapterId,
		'GET',
		null,
		true,
		false,
		(detail) => {
			success(normalizeBookChapter(detail))
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchHomeCategories(
	success : (pageData : PageResponse<HomeCategory>) => void,
	fail : (message : string) => void
) {
	request<PageResponse<HomeCategory>>(
		'/api/v1/admin/content/home/categories?page=1&size=50',
		'GET',
		null,
		true,
		false,
		(pageData) => {
			success(pageData)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchHomeContents(
	success : (pageData : PageResponse<HomeContent>) => void,
	fail : (message : string) => void
) {
	request<PageResponse<HomeContent>>(
		'/api/v1/admin/content/home/contents?page=1&size=50',
		'GET',
		null,
		true,
		false,
		(pageData) => {
			success(pageData)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchAppHome(
	success : (data : AppHomeResponse) => void,
	fail : (message : string) => void
) {
	request<AppHomeResponse>(
		'/api/v1/app/home',
		'GET',
		null,
		true,
		false,
		(data) => {
			success(data)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchTopicDetail(
	id : string,
	success : (detail : Topic) => void,
	fail : (message : string) => void
) {
	request<Topic>(
		'/api/v1/app/learning/topics/' + id,
		'GET',
		null,
		true,
		false,
		(detail) => {
			success(detail)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchProfileFavorites(
	page : number,
	size : number,
	success : (pageData : PageResponse<AppResourceRecord>) => void,
	fail : (message : string) => void,
	sort : string = ''
) {
	let path = '/api/v1/app/profile/favorites?page=' + String(page) + '&size=' + String(size)
	if (sort != null && sort.length > 0) {
		path += '&sort=' + encodeURIComponent(sort)
	}

	request<PageResponse<AppResourceRecord>>(
		path,
		'GET',
		null,
		true,
		false,
		(pageData) => {
			success(pageData)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchLearningHistory(
	page : number,
	size : number,
	success : (pageData : PageResponse<AppResourceRecord>) => void,
	fail : (message : string) => void
) {
	request<PageResponse<AppResourceRecord>>(
		'/api/v1/app/profile/browse-histories?page=' + String(page) + '&size=' + String(size) + '&sort=occurredAt,desc',
		'GET',
		null,
		true,
		false,
		(pageData) => {
			success(pageData)
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchTopicCards(
	page : number,
	size : number,
	keyword : string,
	success : (pageData : PageResponse<TopicCard>) => void,
	fail : (message : string) => void
) {
	let path = '/api/v1/app/learning/topics?page=' + String(page) + '&size=' + String(size)
	if (keyword != null && keyword.length > 0) {
		path += '&keyword=' + encodeURIComponent(keyword)
	}

	request<UTSJSONObject>(
		path,
		'GET',
		null,
		true,
		false,
		(pageData) => {
			const recordsValue = pageData['records']
			const records = recordsValue != null && recordsValue instanceof Array ? (recordsValue as UTSJSONObject[]) : []
			success({
				records: records.map((item : UTSJSONObject) : TopicCard => {
					return normalizeTopicCard(item)
				}),
				total: readNumberField(pageData, 'total'),
				page: readNumberField(pageData, 'page') > 0 ? readNumberField(pageData, 'page') : page,
				size: readNumberField(pageData, 'size') > 0 ? readNumberField(pageData, 'size') : size
			})
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchTopicDetailV2(
	id : string,
	success : (detail : TopicDetail) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/learning/topics/' + id,
		'GET',
		null,
		true,
		false,
		(detail) => {
			success(normalizeTopicDetail(detail))
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchTopicSectionResources(
	topicId : string,
	sectionType : string,
	page : number,
	size : number,
	success : (pageData : PageResponse<TopicSectionResource>) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/learning/topics/' + topicId + '/sections/' + sectionType + '?page=' + String(page) + '&size=' + String(size),
		'GET',
		null,
		true,
		false,
		(pageData) => {
			const recordsValue = pageData['records']
			const records = recordsValue != null && recordsValue instanceof Array ? (recordsValue as UTSJSONObject[]) : []
			success({
				records: records.map((item : UTSJSONObject) : TopicSectionResource => {
					return normalizeTopicSectionResource(item)
				}),
				total: readNumberField(pageData, 'total'),
				page: readNumberField(pageData, 'page') > 0 ? readNumberField(pageData, 'page') : page,
				size: readNumberField(pageData, 'size') > 0 ? readNumberField(pageData, 'size') : size
			})
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchArticles(
	page : number,
	size : number,
	keyword : string,
	success : (pageData : PageResponse<Article>) => void,
	fail : (message : string) => void
) {
	let path = '/api/v1/app/content/articles?page=' + String(page) + '&size=' + String(size)
	if (keyword != null && keyword.length > 0) {
		path += '&keyword=' + encodeURIComponent(keyword)
	}

	request<UTSJSONObject>(
		path,
		'GET',
		null,
		true,
		false,
		(pageData) => {
			const recordsValue = pageData['records']
			const records = recordsValue != null && recordsValue instanceof Array ? (recordsValue as UTSJSONObject[]) : []
			success({
				records: records.map((item : UTSJSONObject) : Article => {
					return normalizeArticle(item)
				}),
				total: readNumberField(pageData, 'total'),
				page: readNumberField(pageData, 'page') > 0 ? readNumberField(pageData, 'page') : page,
				size: readNumberField(pageData, 'size') > 0 ? readNumberField(pageData, 'size') : size
			})
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchArticleDetail(
	id : string,
	success : (detail : Article) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/content/articles/' + id,
		'GET',
		null,
		true,
		false,
		(detail) => {
			success(normalizeArticle(detail))
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchExamPapers(
	page : number,
	size : number,
	success : (pageData : PageResponse<ExamPaper>) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/learning/exam-papers?page=' + String(page) + '&size=' + String(size),
		'GET',
		null,
		true,
		false,
		(pageData) => {
			const recordsValue = pageData['records']
			const records = recordsValue != null && recordsValue instanceof Array ? (recordsValue as UTSJSONObject[]) : []
			success({
				records: records.map((item : UTSJSONObject) : ExamPaper => {
					return normalizeExamPaper(item)
				}),
				total: readNumberField(pageData, 'total'),
				page: readNumberField(pageData, 'page') > 0 ? readNumberField(pageData, 'page') : page,
				size: readNumberField(pageData, 'size') > 0 ? readNumberField(pageData, 'size') : size
			})
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchExamPaperDetail(
	id : string,
	success : (paper : ExamPaper) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/learning/exam-papers/' + id,
		'GET',
		null,
		true,
		false,
		(detail) => {
			success(normalizeExamPaper(detail))
		},
		(message) => {
			fail(message)
		}
	)
}

export function submitExamPaper(
	id : string,
	data : ExamSubmitRequest,
	success : (record : ExamRecord) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/learning/exam-papers/' + id + '/submit',
		'POST',
		data as UTSJSONObject,
		true,
		false,
		(record) => {
			success(normalizeExamRecord(record))
		},
		(message) => {
			fail(message)
		}
	)
}

export function fetchExamRecordDetail(
	id : string,
	success : (record : ExamRecord) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/learning/exam-records/' + id,
		'GET',
		null,
		true,
		false,
		(record) => {
			success(normalizeExamRecord(record))
		},
		(message) => {
			fail(message)
		}
	)
}

export function reportBrowseHistory(
	data : BrowseHistoryRequest,
	success : () => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/interaction/browse-histories',
		'POST',
		data as UTSJSONObject,
		true,
		true,
		() => {
			success()
		},
		(message) => {
			fail(message)
		}
	)
}

function normalizeFavoriteResourceType(resourceType : string) : string {
	if (resourceType == null || resourceType.length == 0) {
		return ''
	}

	const normalized = resourceType.toLowerCase()
	if (normalized == 'audio' || normalized == 'podcast') {
		return 'AUDIO'
	}
	if (normalized == 'course') {
		return 'COURSE'
	}
	if (normalized == 'live') {
		return 'LIVE'
	}
	if (normalized == 'info' || normalized == 'topic' || normalized == 'article') {
		return 'INFO'
	}
	if (normalized == 'knowledge') {
		return 'KNOWLEDGE'
	}
	return resourceType.toUpperCase()
}

function isFavoriteResourceTypeMatched(left : string, right : string) : boolean {
	return normalizeFavoriteResourceType(left) == normalizeFavoriteResourceType(right)
}

export function checkFavoriteStatus(
	resourceType : string,
	resourceId : number,
	success : (favorited : boolean) => void,
	fail : (message : string) => void
) {
	fetchProfileFavorites(
		1,
		100,
		(pageData) => {
			const records = pageData.records != null ? pageData.records : []
			const matched = records.some((item : AppResourceRecord) : boolean => {
				return isFavoriteResourceTypeMatched(item.resourceType, resourceType) && item.resourceId == resourceId
			})
			success(matched)
		},
		(message) => {
			fail(message)
		}
	)
}

export function updateFavoriteStatus(
	data : AppFavoriteRequest,
	success : (result : AppResourceInteraction) => void,
	fail : (message : string) => void
) {
	request<AppResourceInteraction>(
		'/api/v1/app/interaction/favorites',
		'POST',
		data as UTSJSONObject,
		true,
		false,
		(result) => {
			success(result)
		},
		(message) => {
			fail(message)
		}
	)
}

export function submitFeedback(
	data : AppFeedbackRequest,
	success : () => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/interaction/feedbacks',
		'POST',
		data as UTSJSONObject,
		true,
		true,
		() => {
			success()
		},
		(message) => {
			fail(message)
		}
	)
}

export function requestAvatarUploadUrl(
	requestData : AvatarUploadUrlRequest,
	success : (data : AvatarUploadUrlResponse) => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/profile/avatar/upload-url',
		'POST',
		requestData as UTSJSONObject,
		true,
		false,
		(data) => {
			const normalized = normalizeUploadUrlResponse(data)
			if (normalized.uploadUrl == '' || normalized.objectKey == '') {
				fail('头像上传地址返回格式不正确')
				return
			}
			success(normalized)
		},
		(message) => {
			fail(message)
		}
	)
}

export function confirmAvatarUpload(
	data : AvatarConfirmRequest,
	success : (result : AppProfile) => void,
	fail : (message : string) => void
) {
	request<AppProfile>(
		'/api/v1/app/profile/avatar/confirm',
		'POST',
		data as UTSJSONObject,
		true,
		false,
		(result) => {
			success(result)
		},
		(message) => {
			fail(message)
		}
	)
}

export function uploadAvatarFile(
	filePath : string,
	config : AvatarUploadUrlResponse,
	success : () => void,
	fail : (message : string) => void
) {
	if (config.method.toUpperCase() == 'PUT') {
		uni.uploadFile({
			url: config.uploadUrl,
			filePath,
			name: 'file',
			header: (() : UTSJSONObject => {
				const putHeaders : UTSJSONObject = {
					'Content-Type': 'application/octet-stream'
				}
				if (config.headers != null) {
					const keys = Object.keys(config.headers)
					for (let i = 0; i < keys.length; i++) {
						const key = keys[i]
						const value = config.headers[key]
						if (typeof value == 'string') {
							putHeaders[key] = value
						}
					}
				}
				return putHeaders
			})(),
			success: (res) => {
				if (res.statusCode >= 200 && res.statusCode < 300) {
					success()
					return
				}
				fail('头像文件上传失败')
			},
			fail: (err) => {
				fail(err.errMsg != null && err.errMsg != '' ? err.errMsg : '头像文件上传失败')
			}
		})
		return
	}

	const uploadHeader : UTSJSONObject = {}
	if (config.headers != null) {
		const keys = Object.keys(config.headers)
		for (let i = 0; i < keys.length; i++) {
			const key = keys[i]
			const value = config.headers[key]
			if (typeof value == 'string') {
				uploadHeader[key] = value
			}
		}
	}

	const uploadFormData : UTSJSONObject = {}
	if (config.formData != null) {
		const keys = Object.keys(config.formData)
		for (let i = 0; i < keys.length; i++) {
			const key = keys[i]
			const value = config.formData[key]
			if (typeof value == 'string') {
				uploadFormData[key] = value
			}
		}
	}

	uni.uploadFile({
		url: config.uploadUrl,
		filePath,
		name: 'file',
		formData: uploadFormData,
		header: uploadHeader,
		success: (res) => {
			if (res.statusCode >= 200 && res.statusCode < 300) {
				success()
				return
			}
			fail('头像文件上传失败')
		},
		fail: (err) => {
			fail(err.errMsg != null && err.errMsg != '' ? err.errMsg : '头像文件上传失败')
		}
	})
}

export function uploadAvatarBinaryFile(
	filePath : string,
	config : AvatarUploadUrlResponse,
	binaryConfig : AvatarBinaryUploadConfig,
	success : () => void,
	fail : (message : string) => void
) {
	if (config.method.toUpperCase() == 'PUT') {
		uni.getFileSystemManager().readFile({
			filePath,
			success: (readRes) => {
				const putHeaders : UTSJSONObject = {
					'Content-Type': binaryConfig.contentType
				}
				if (config.headers != null) {
					const keys = Object.keys(config.headers)
					for (let i = 0; i < keys.length; i++) {
						const key = keys[i]
						const value = config.headers[key]
						if (typeof value == 'string') {
							putHeaders[key] = value
						}
					}
				}
				uni.request({
					url: config.uploadUrl,
					method: 'PUT',
					data: readRes.data,
					header: putHeaders,
					success: (res) => {
						if (res.statusCode >= 200 && res.statusCode < 300) {
							success()
							return
						}
						fail('头像文件上传失败')
					},
					fail: (err) => {
						fail(err.errMsg != null && err.errMsg != '' ? err.errMsg : '头像文件上传失败')
					}
				})
			},
			fail: (err) => {
				fail(err.errMsg != null && err.errMsg != '' ? err.errMsg : '读取头像文件失败')
			}
		})
		return
	}

	const uploadHeader : UTSJSONObject = {}
	if (config.headers != null) {
		const keys = Object.keys(config.headers)
		for (let i = 0; i < keys.length; i++) {
			const key = keys[i]
			const value = config.headers[key]
			if (typeof value == 'string') {
				uploadHeader[key] = value
			}
		}
	}

	const uploadFormData : UTSJSONObject = {}
	if (config.formData != null) {
		const keys = Object.keys(config.formData)
		for (let i = 0; i < keys.length; i++) {
			const key = keys[i]
			const value = config.formData[key]
			if (typeof value == 'string') {
				uploadFormData[key] = value
			}
		}
	}

	uni.uploadFile({
		url: config.uploadUrl,
		filePath,
		name: 'file',
		formData: uploadFormData,
		header: uploadHeader,
		success: (res) => {
			if (res.statusCode >= 200 && res.statusCode < 300) {
				success()
				return
			}
			fail('头像文件上传失败')
		},
		fail: (err) => {
			fail(err.errMsg != null && err.errMsg != '' ? err.errMsg : '头像文件上传失败')
		}
	})
}

export function clearAuthStorage() {
	uni.removeStorageSync(ACCESS_TOKEN_KEY)
	uni.removeStorageSync(TOKEN_TYPE_KEY)
	uni.removeStorageSync(LOGIN_USER_KEY)
	uni.removeStorageSync(CURRENT_USER_KEY)
	uni.removeStorageSync(BIND_TOKEN_KEY)
}

export function getCurrentUserFromStorage() : CurrentUser | null {
	const user = uni.getStorageSync(CURRENT_USER_KEY)
	if (user == null || user == '') {
		return null
	}
	return normalizeCurrentUserData(user as UTSJSONObject)
}

export function hasToken() : boolean {
	return getAccessToken() != ''
}

export function hasBoundMobile(user : CurrentUser | null) : boolean {
	if (user == null) {
		return false
	}
	return user.mobile != null && user.mobile != ''
}

function saveBindToken(token : string) {
	uni.setStorageSync(BIND_TOKEN_KEY, token)
}

export function getBindToken() : string {
	const token = uni.getStorageSync(BIND_TOKEN_KEY)
	return typeof token == 'string' ? token : ''
}

export function clearBindToken() {
	uni.removeStorageSync(BIND_TOKEN_KEY)
}

function redirectToLogin() {
	uni.showToast({
		title: '请先登录',
		icon: 'none'
	})
	uni.navigateTo({
		url: '/pages/login/index'
	})
}

function request<T>(
	path : string,
	method : string,
	data : UTSJSONObject | null,
	needAuth : boolean,
	allowEmptyData : boolean,
	success : (data : T) => void,
	fail : (message : string) => void
) {
	const header : UTSJSONObject = {
		'Content-Type': 'application/json'
	}
	if (needAuth) {
		const authorization = buildAuthorization()
		if (authorization == '') {
			clearAuthStorage()
			redirectToLogin()
			fail('登录状态已失效，请重新登录')
			return
		}
		header['Authorization'] = authorization
	}

	uni.request({
		url: BASE_URL + path,
		method,
		data,
		header,
		success: (res) => {
			const statusCode = res.statusCode
			const body = res.data as ApiResponse<T>
			if (statusCode == 401 || (body != null && body.code == 'UNAUTHORIZED')) {
				clearAuthStorage()
				redirectToLogin()
				fail(body != null && body.message != null && body.message != '' ? body.message : '登录状态已失效，请重新登录')
				return
			}
			if (statusCode >= 200 && statusCode < 300 && body != null && body.success == true) {
				if (body.data != null) {
					success(body.data)
					return
				}
				if (allowEmptyData) {
					success(null as T)
					return
				}
			}

			if (body != null && body.message != null && body.message != '') {
				fail(body.message)
				return
			}

			fail('请求失败，请稍后重试')
		},
		fail: (err) => {
			fail(err.errMsg != null && err.errMsg != '' ? err.errMsg : '网络请求失败')
		}
	})
}

export function fetchCurrentUser(
	success : (user : CurrentUser) => void,
	fail : (message : string) => void
) {
	request<CurrentUser>(
		'/api/v1/app/auth/me',
		'GET',
		null,
		true,
		false,
		(user) => {
			user.avatarUrl = normalizeAppUrl(user.avatarUrl)
			saveCurrentUser(user)
			success(user)
		},
		(message) => {
			fail(message)
		}
	)
}

export function loginByWechat(
	nickname : string,
	avatarUrl : string,
	success : (user : CurrentUser | null) => void,
	fail : (message : string) => void
) {
	// #ifdef MP-WEIXIN
	uni.login({
		success: (loginRes) => {
			const requestBody : UTSJSONObject = {
				code: loginRes.code,
				nickname,
				avatarUrl
			}
			request<WechatLoginData>(
				'/api/v1/app/auth/wechat-login',
				'POST',
				requestBody,
				false,
				false,
				(loginData) => {
					if (loginData.needBindMobile) {
						saveBindToken(loginData.bindToken)
						success(null)
						return
					}
					const saveData : LoginResponseData = {
						tokenType: loginData.tokenType,
						accessToken: loginData.accessToken,
						expiresIn: loginData.expiresIn,
						user: loginData.user
					}
					saveLogin(saveData)
					fetchCurrentUser(
						(user) => {
							success(user)
						},
						(message) => {
							fail(message)
						}
					)
				},
				(message) => {
					fail(message)
				}
			)
		},
		fail: (err) => {
			fail(err.errMsg != null && err.errMsg != '' ? err.errMsg : '微信登录失败')
		}
	})
	// #endif

	// #ifndef MP-WEIXIN
	fail('请在微信小程序环境中使用微信授权登录')
	// #endif
}

export function wechatBindMobile(
	bindToken : string,
	mobile : string,
	code : string,
	success : (data : LoginResponseData) => void,
	fail : (message : string) => void
) {
	const requestBody : UTSJSONObject = {
		bindToken,
		mobile,
		code
	}
	request<LoginResponseData>(
		'/api/v1/app/auth/wechat-bind-mobile',
		'POST',
		requestBody,
		false,
		false,
		(loginData) => {
			success(loginData)
		},
		(message) => {
			fail(message)
		}
	)
}

export function sendSmsCode(
	mobile : string,
	success : () => void,
	fail : (message : string) => void
) {
	const requestBody : UTSJSONObject = {
		mobile
	}
	request<UTSJSONObject>(
		'/api/v1/app/auth/sms-code',
		'POST',
		requestBody,
		false,
		true,
		() => {
			success()
		},
		(message) => {
			fail(message)
		}
	)
}

export function logout(
	success : () => void,
	fail : (message : string) => void
) {
	request<UTSJSONObject>(
		'/api/v1/app/auth/logout',
		'POST',
		null,
		true,
		true,
		() => {
			clearAuthStorage()
			success()
		},
		(message) => {
			fail(message)
		}
	)
}

export function loginBySms(
	mobile : string,
	code : string,
	success : (user : CurrentUser) => void,
	fail : (message : string) => void
) {
	const requestBody : UTSJSONObject = {
		mobile,
		code
	}
	request<LoginResponseData>(
		'/api/v1/app/auth/sms-login',
		'POST',
		requestBody,
		false,
		false,
		(loginData) => {
			saveLogin(loginData)
			fetchCurrentUser(
				(user) => {
					success(user)
				},
				(message) => {
					fail(message)
				}
			)
		},
		(message) => {
			fail(message)
		}
	)
}
