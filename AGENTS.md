# Uni-App 协作范围说明

## 目标

本项目当前阶段以 `uni-app x` 小程序端开发为主，重点是：

- 完成小程序端页面内容填充与交互串联。
- 按后端接口文档接入真实 API。
- 在不破坏既有 UI 设计的前提下补齐登录、认证、资料、列表、详情等业务流程。

## 当前工作范围

当前允许推进的工作仅限以下方向：

- 小程序端页面内容开发。
- 小程序端页面之间的跳转、参数传递、登录态控制。
- 小程序端接口封装、请求调用、错误处理、登录态缓存。
- 基于后端 `api.json` 的接口联调。
- 与现有页面对应的数据绑定、空态、加载态、提交态处理。
- 在不改变既有视觉稿的前提下补充必要的交互文案与状态反馈。

## 明确不做的内容

以下内容默认不在当前协作范围内，除非产品或负责人明确提出：

- 不随意重做、改版、重排已经完成的 UI 页面。
- 不主动修改现有页面的布局结构、主色、字号体系、间距体系、图片资源。
- 不擅自替换已存在的组件风格。
- 不扩展管理端、PC 端、后台端功能。
- 不修改后端接口协议定义。
- 不凭猜测新增接口字段、状态码语义或业务规则。
- 不编辑 `unpackage/` 下的构建产物。

## UI 协作约束

已有 UI 视为已确认资产，默认按“只接逻辑，不动样式主体”执行：

- 可以补充 `loading`、`disabled`、`toast`、`empty`、`error` 等必要状态。
- 可以为接口联调增加极小范围的属性绑定或显隐控制。
- 可以修复明显影响功能的 UI bug。
- 若改动会影响视觉表现，必须先确认再改。
- 若只是为了“更好看”“更统一”“更现代”，一律不改。

## 接口接入约束

接口开发必须遵守以下规则：

- 以后端 `api.json` 为唯一接口依据。
- 请求路径、请求体、响应字段、鉴权方式必须与文档一致。
- 文档中没有的接口，不要自行假设存在。
- 文档缺失时，先记录缺口，再与后端确认。
- 公共请求逻辑应集中封装，避免散落在页面中重复实现。
- Token、用户信息、登录态缓存应统一管理。
- 接口异常必须给出前端可见反馈，不能静默失败。

## 页面改动边界

当前优先处理的小程序端页面包括但不限于：

- `pages/login/*`
- `pages/auth/*`
- `pages/index/*`
- `pages/mine/*`
- `pages/profile/*`

在这些页面内：

- 允许补充脚本逻辑、接口调用、状态控制。
- 允许增加与业务直接相关的轻量工具文件。
- 允许增加统一的请求层、鉴权层、存储层。
- 不允许借机重构整套 UI。
- 不允许把单页任务扩展成全局大改。

## 多人协作规则

- 修改前先确认目标文件是否属于当前任务范围。
- 如果发现他人已完成 UI，优先复用，不重复造轮子。
- 如果发现接口未定、字段不明确、流程冲突，先停在边界处，不硬写。
- 不覆盖、不回退、不清理他人已完成内容，除非得到明确授权。
- 如需新增公共文件，命名要清晰，职责要单一。
- 提交内容应尽量聚焦单一目标，避免把“页面开发”和“样式重构”混在一起。

## 建议的最小交付方式

每次开发尽量按以下粒度推进：

- 先补页面静态内容与交互入口。
- 再接对应接口。
- 再处理登录态、加载态、错误态。
- 最后做最小范围自测。

## 提交前检查

- 页面 UI 主体是否被无意改动。
- 是否只修改了当前任务相关文件。
- 是否误改了 `unpackage/` 产物。
- 接口是否完全依据 `api.json`。
- 缺失接口是否已明确标注，而不是前端自行假设。
- 页面是否具备基本的成功、失败、加载反馈。

## 当前执行原则

本阶段默认执行原则：

- 以“小程序端页面内容 + 后端接口接入”为核心。
- 以“少改 UI、多接逻辑”为基本策略。
- 以“接口文档优先、多人协作安全优先”为最高约束。
